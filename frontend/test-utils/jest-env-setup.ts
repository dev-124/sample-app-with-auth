// This file gets run automatically by Jest before every test
// therefore you don’t need to add the imports to every single test file.
import '@testing-library/jest-dom/extend-expect'

// attach all the DOM Testing Library methods directly onto puppeteer's ElementHandle
// import 'pptr-testing-library/extend'
